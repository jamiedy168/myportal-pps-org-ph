location = /health {
    access_log off;
    return 200 "OK\n";
    add_header Content-Type text/plain;
}

location = /ping {
    access_log off;
    return 200 "pong\n";
    add_header Content-Type text/plain;
}
