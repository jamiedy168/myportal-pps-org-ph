# Installs the PostgreSQL 15 client tools (pg_dump, pg_restore, psql)
# on every Elastic Beanstalk EC2 instance.
# Required for the Database Backup feature in the admin panel.
# Adjust the package version to match your RDS PostgreSQL engine version.

packages:
  yum:
    postgresql15: []
